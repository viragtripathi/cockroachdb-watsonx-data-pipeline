apiVersion: 1

datasources:
  - name: CDC Pipeline
    uid: cdc-pipeline
    type: yesoreyeram-infinity-datasource
    access: proxy
    isDefault: true
    jsonData:
      global_queries: []
    editable: true
